export const state = () => ({
    lableMovie:''
})

export const actions = {
     
}

export const mutations = {
  changeLable(state, index) {
    state.lableMovie = index
  },
}

export const getters = {}
